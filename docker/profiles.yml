anp_fuel:
  target: docker
  outputs:
    docker:
      type: postgres
      host: postgres
      port: 5432
      user: "{{ env_var('DB_USER') }}"
      password: "{{ env_var('DB_PASSWORD') }}"
      dbname: "{{ env_var('DB_NAME') }}"
      schema: public
      threads: 4